[build-system]
requires = ["setuptools>=50", "wheel>=0.37"]
build-backend = "setuptools.build_meta"

[tool.mypy]
disallow_untyped_defs = true

[[tool.mypy.overrides]]
module = [
    "h5py.*",
    "matplotlib.*",
    "pandas.*",
    "scipy.*",
]
ignore_missing_imports = true
